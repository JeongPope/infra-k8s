apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nginx-service
  namespace: project
spec:
  hosts:
    - { { your domain } } # change your domain
  gateways:
    - istio-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: nginx-svc
            port:
              number: 8080
