apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: istio-gateway
  namespace: project
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - dev.popehub.net # change your domain
      port:
        name: http
        protocol: HTTP
        number: 80
