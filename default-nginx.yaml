apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: workspace
spec:
  selector:
    app: nginx
    version: v1
  ports:
    - port: 8080
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: workspace
  labels:
    app: nginx
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
      version: v1
  template:
    metadata:
      labels:
        app: nginx
        version: v1
    spec:
      containers:
        - image: nginx
          name: nginx
          resources:
            requests:
              cpu: 25m
              memory: 100Mi
            limits:
              cpu: 50m
              memory: 200Mi
