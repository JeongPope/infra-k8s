apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: istio-gateway
  namespace: workspace
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - devops.popehub.net # change your domain
      port:
        name: http
        protocol: HTTP
        number: 80
