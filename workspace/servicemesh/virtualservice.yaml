apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nginx-service
  namespace: workspace
spec:
  hosts:
    - <your hosts> # need to change
  gateways:
    - istio-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: nginx-svc
            port:
              number: 8080
